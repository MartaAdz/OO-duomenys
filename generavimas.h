#ifndef generavimas_H
#define generavimas_H
#include <vector>
#include "studentas.h"
    void studentu_generavimas(unsigned int stud_kiekis, int paz_kiekis);
    void iverciai(studentas &S, int kiekis);
    std::vector<int> ivesti_pazymiai(studentas &stud);
#endif
